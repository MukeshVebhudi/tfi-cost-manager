# ChitraKhata MVP

A responsive film-production budget and expense dashboard designed for the Telugu Film Industry.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000. This MVP uses in-memory demo data; adding an expense works for the current session. A production release should connect authentication, PostgreSQL storage, receipt uploads, and an immutable audit log.
